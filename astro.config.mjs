import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://www.my-site.dev', // Your final, deployed URL
  base: '/foo', // The base path to deploy to
});
